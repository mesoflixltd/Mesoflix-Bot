import { observer } from 'mobx-react-lite';

const AIHub = observer(() => {
    return (
        <div className='ai-hub-page' style={{ paddingTop: '12px', paddingLeft: '16px' }}>
            <h1>AI Hub</h1>
            <p>Welcome to the AI Hub page. This is a placeholder for future content.</p>
        </div>
    );
});

export default AIHub;
