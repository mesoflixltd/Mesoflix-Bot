import { observer } from 'mobx-react-lite';

const DCircles = observer(() => {
    return (
        <div className='dcircles-page' style={{ paddingTop: '12px', paddingLeft: '16px' }}>
            <h1>DCircles</h1>
            <p>Welcome to the DCircles page. This is a placeholder for future content.</p>
        </div>
    );
});

export default DCircles;
