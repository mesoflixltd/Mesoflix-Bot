import { observer } from 'mobx-react-lite';

const FreeBots = observer(() => {
    return (
        <div className='freebots-page' style={{ paddingTop: '12px', paddingLeft: '16px' }}>
            <h1>FreeBots</h1>
            <p>Welcome to the FreeBots page. This is a placeholder for future content.</p>
        </div>
    );
});

export default FreeBots;
