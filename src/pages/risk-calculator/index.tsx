import { observer } from 'mobx-react-lite';

const RiskCalculator = observer(() => {
    return (
        <div className='risk-calculator-page' style={{ paddingTop: '12px', paddingLeft: '16px' }}>
            <h1>Risk Calculator</h1>
            <p>Welcome to the Risk Calculator page. This is a placeholder for future content.</p>
        </div>
    );
});

export default RiskCalculator;
